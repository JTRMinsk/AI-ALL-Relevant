# -----------------------------------------------------------------------------
# Obsidian 库：只做「自己的笔记与附件」版本控制
# 以下目录多为编辑器/插件配置与运行时状态，进 Git 只会制造噪音且无多端同步诉求
# -----------------------------------------------------------------------------

# Obsidian 本体：核心设置、外观、快捷键、主题片段、community-plugins.json、
# 各插件安装在 .obsidian/plugins/ … 整块排除即可；以后新装插件也都在此目录内。
.obsidian/

# Claudian 等插件：常在库根建独立文件夹，会话/缓存/本地偏好混放，整块不跟踪更安全。
.claudian/

# 若启用「库内回收站」（或主题将其指向 vault 根下的 .trash）
.trash/

# -----------------------------------------------------------------------------
# 系统与同步杂项（与笔记内容无关）
# -----------------------------------------------------------------------------

.DS_Store
desktop.ini
Thumbs.db

# Office / OneDrive 等产生的临时锁定文件（库在 OneDrive 下易出现）
~$*

# -----------------------------------------------------------------------------
# （可选）用 Cursor / VS Code 打开整个库且不想提交工作区编辑器配置时再取消注释
# -----------------------------------------------------------------------------
# .cursor/
# .vscode/
